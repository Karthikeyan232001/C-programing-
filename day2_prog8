#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>

int main() {
    int L, B;
    int l1, b1;
    int l2, b2;
    scanf("%d %d", &L, &B);
    scanf("%d %d", &l1, &b1);
    scanf("%d %d", &l2, &b2);
    int canFit = 0;
    int orientations[2][2] = {{l1, b1}, {b1, l1}};
    int orientations2[2][2] = {{l2, b2}, {b2, l2}};

    for(int i = 0; i < 2; i++) {
        int p1_l = orientations[i][0];
        int p1_b = orientations[i][1];

        for(int j = 0; j < 2; j++) {
            int p2_l = orientations2[j][0];
            int p2_b = orientations2[j][1];
            if ((p1_l + p2_l <= L) && (p1_b <= B) && (p2_b <= B)) {
                canFit = 1;
            }
            else if ((p1_b + p2_b <= B) && (p1_l <= L) && (p2_l <= L)) {
                canFit = 1;
            }
        }
    }

    if (canFit)
        printf("Raj can fix both painting\n");
    else
        printf("Raj cannot fix both painting\n");
  
  
    return 0;
}
